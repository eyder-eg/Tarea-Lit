import { css } from "lit";

const styles = css`
  :host {
    font-family: var(--font-family);
  }
  button {
    background-color: #fc0000;
    color: white;
    border: 0;
    padding: 0.5rem 1rem;
    border-radius: 4px;
    margin-left: 1rem;
    cursor: pointer;
  }
`;

export default styles;
