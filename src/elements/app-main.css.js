import { css } from "lit";

const styles = css`
  :host {
    font-family: var(--font-family);
  }

  main {
    display: flex;
    flex-direction: column;
    align-items: center;
    
  }
`;

export default styles;
